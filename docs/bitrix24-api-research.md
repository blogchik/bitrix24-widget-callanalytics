# Bitrix24 REST API — verified research notes

Collected 2026-09-07 by four parallel research agents reading the official documentation
(apidocs.bitrix24.com / apidocs.bitrix24.ru / dev.1c-bitrix.ru/rest_help / helpdesk.bitrix24.com).
Each fact is marked `verified` (read on an official page), `likely` (secondary or partial source)
or `unverified`. The **Corrections to brief** blocks list every place the official docs contradict
the original project brief; those corrections are already folded into `docs/architecture.md`.

---

## Research block
- [verified] (user.current — scope and fields) user.current requires scope user, user_brief or user_basic (any of the three). Executable by any user. Returned fields depend on the app's scope; documented sample response includes ID, ACTIVE, NAME, LAST_NAME, EMAIL, LAST_LOGIN, DATE_REGISTER, IS_ONLINE, LAST_ACTIVITY_DATE, PERSONAL_GENDER, PERSONAL_BIRTHDAY, WORK_POSITION, UF_EMPLOYMENT_DATE, UF_DEPARTMENT. Under user_brief the EMAIL/contact fields are stripped (see user-scope fact).
  - src: https://apidocs.bitrix24.com/api-reference/user/user-current.html
  - impact: With only user_brief granted, resolve the current user via user.current using the request's AUTH_ID. Store the resolved ID as the per-request principal; never trust a user id passed in from the iframe. Do not depend on EMAIL being present.
- [verified] (user.admin — scope and semantics) user.admin is in the 'basic' scope (no explicit scope needed; any user may call it, listed under Common > Users). Returns boolean: true if the current user has permissions to manage application settings, false otherwise. The 'profile' method (also basic scope, any user) returns ID, ADMIN, NAME, LAST_NAME, PERSONAL_GENDER, TIME_ZONE, PERSONAL_PHOTO, and its ADMIN field 'matches the result of the user.admin method'.
  - src: https://apidocs.bitrix24.com/api-reference/common/users/user-admin.html
  - impact: Server-side admin check = user.admin (or profile.ADMIN) with the current user's AUTH_ID; both work without adding scopes. Cache the result per (member_id, user_id) for a short TTL. Never use BX24.isAdmin() from the iframe as the authorization source — it is a client-side JS SDK function only (https://apidocs.bitrix24.com/sdk/bx24-js-sdk/additional-functions/bx24-is-admin.html).
- [verified] (user.access — access codes) user.access (basic scope, any user) takes ACCESS: array of access codes (U<id> user, G<id> group, AU all authorized users; names via access.name) and returns true if the current user has at least one code or has application-settings-management rights. For admins it 'always returns true, regardless of the access codes provided'. There is no documented access code for telephony roles/permissions; telephony rights are not exposed through user.access.
  - src: https://apidocs.bitrix24.com/api-reference/common/users/user-access.html
  - impact: user.access is not useful for telephony permission detection; do not build on it. Only user.admin + probing voximplant.statistic.get are usable.
- [verified] (user_brief scope — methods and fields) user_brief enables user.get, user.search, user.current, user.fields; user.add and user.update are NOT available in user_brief/user_basic. user_brief = 'user information without contact details'. Fields under user_brief include ID, XML_ID, ACTIVE, NAME, LAST_NAME, SECOND_NAME, TITLE, IS_ONLINE, TIME_ZONE, PERSONAL_PHOTO, TIMESTAMP_X, DATE_REGISTER, LAST_ACTIVITY_DATE, PERSONAL_GENDER, PERSONAL_BIRTHDAY, WORK_POSITION, WORK_COMPANY, WORK_DEPARTMENT, UF_DEPARTMENT, UF_PHONE_INNER, UF_EMPLOYMENT_DATE, UF_TIMEMAN, etc. user_basic adds EMAIL, PERSONAL_PHONE, WORK_PHONE. (The user_brief list on the page inconsistently also lists PERSONAL_PHONE/WORK_PHONE/PERSONAL_MOBILE; treat contact fields as unavailable.)
  - src: https://apidocs.bitrix24.com/api-reference/user/user-scope.html
  - impact: user.get IS callable with user_brief only. Employee cache table needs: id, name, last_name, second_name, personal_photo, work_position, active, uf_department (array), uf_phone_inner. Do not model email/phone columns.
- [verified] (user.get — filtering, pagination, ADMIN_MODE) user.get: scope user/user_brief/user_basic; 'Who can execute: any user'; page size fixed at 50, start=(N-1)*50; default sort ID ASC, SORT/ORDER params. FILTER supports array values and prefix operators (@ for IN, >=, >, <=, <, %, !=), e.g. '@ID': [..], ACTIVE, UF_DEPARTMENT, IS_ONLINE, USER_TYPE (employee/extranet/email). ACTIVE=true 'excludes terminated users from the request'. ADMIN_MODE (boolean): 'Key for operation in administrator mode. Used to obtain data about any users' — no explicit statement of who may set it. Documented errors include ACCESS_DENIED, INVALID_CREDENTIALS, insufficient_scope, expired_token, QUERY_LIMIT_EXCEEDED, user_access_error, PORTAL_DELETED.
  - src: https://apidocs.bitrix24.com/api-reference/user/user-get.html
  - impact: Employee sync: run user.get with the stored admin token, FILTER {'@ID': [ids]} in chunks of 50 IDs, or full paging with ADMIN_MODE=true, wrapped in batch (50 commands). Do NOT filter ACTIVE=true for the name cache — dismissed users still have historical calls and must resolve to names; store ACTIVE and show 'dismissed' badge instead. Use USER_TYPE=employee if extranet/email users should be excluded from the employee filter.
- [verified] (user.search) user.search (scope user/user_brief/user_basic) filters by NAME, LAST_NAME, WORK_POSITION, UF_DEPARTMENT_NAME, USER_TYPE or FIND (fulltext); FIND cannot be combined with other params; 50 per page; ADMIN_MODE supported. It does not filter by ID list.
  - src: https://apidocs.bitrix24.com/api-reference/user/user-search.html
  - impact: Use user.get, not user.search, for ID-based bulk resolution. user.search is only useful for a typeahead employee picker, which v1 can serve from the local cache instead.
- [verified] (voximplant.statistic.get — permission requirement and error code) Scope telephony. 'Who can execute the method: user with Call Statistics — View permission' (RU: 'пользователь с правом Статистика звонков — Просмотр'). Method-specific error: ACCESS_DENIED — 'Insufficient permissions to view call statistics'. System errors: NO_AUTH_FOUND (401), INVALID_CREDENTIALS (403), QUERY_LIMIT_EXCEEDED (503), OPERATION_TIME_LIMIT (429), INTERNAL_SERVER_ERROR/ERROR_UNEXPECTED_ANSWER (500). Response has total ('Total number of records in the selection') and next ('Offset for the next page'). Sorting allowed by all filter fields except CALL_TYPE. FILTER example: {'ID':[1,7], '>=CALL_START_DATE': '2025-01-01T00:00:00+01:00'}.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: The only REST way to learn whether a user has telephony statistics rights is to call voximplant.statistic.get with THAT user's AUTH_ID and check for error == 'ACCESS_DENIED' → render the explicit no-access state. A user with 'own calls' scope gets HTTP 200 with a filtered result (not an error), so a probe call succeeding does not mean 'view all'. Implement: probe = statistic.get(FILTER minimal, limit 1) per user with short TTL cache keyed (member_id,user_id); treat ACCESS_DENIED distinctly from expired_token/insufficient_scope.
- [verified] (No REST method exposes telephony roles/permissions) The telephony method index (voximplant.callback.start, infocall.*, tts.voices.get, url.get, statistic.get, sip.*, line.*, user.get, user.activatePhone) contains no method that returns a user's telephony role or 'Call statistics' permission level; per-method rights are 'depending on the method'. voximplant.user.get returns only SIP settings (ID, DEFAULT_LINE, PHONE_ENABLED, SIP_SERVER, SIP_LOGIN, SIP_PASSWORD, INNER_NUMBER), requires 'User Settings — Modify' permission and 'Applications require administrator confirmation' (METHOD_CONFIRM_WAITING) — not a permission lookup.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/index.html
  - impact: Cannot determine 'all / department / own' level for a non-admin user through REST. The brief's simplified mapping (admin → all; non-admin → own rows only; ACCESS_DENIED → no-access state) is the only implementable, moderation-safe model. Do not call voximplant.user.get (it triggers an admin-confirmation flow).
- [verified] (Scope does not override user rights) 'A scope does not grant access to all data by itself. The application will only see the data that the user executing the request has access to.'
  - src: https://apidocs.bitrix24.com/api-reference/index.html
  - impact: Confirms the brief's split: sync with the installing admin's token (full history), read with the current user's token semantics. Note the install token's data visibility depends on that admin's telephony rights — portal admins always have full telephony access (see helpdesk fact), so requiring admin-only install guarantees full-history sync.
- [verified] (Product-level telephony permissions (helpdesk)) Telephony access permissions are available on all commercial cloud plans and On-Premise, configured at CRM > Sales > Sales channels > Telephony > Connection > Access permissions. Default roles: Administrator (make calls, listen to recordings, view all calls, manage settings), Chief executive (view all calls), Head of department (view calls from their department), Manager ('Can work only with their own calls'). Permission 'Call statistics' values: Only their own calls / Calls from their department / Any calls / No access. 'Call Recording: Listen': Their own / department / Any / No access. Roles are assigned to users, departments, groups; with multiple roles the highest access applies.
  - src: https://helpdesk.bitrix24.com/open/17441038/
  - impact: There is no 'Operator' role by default (brief's terminology is wrong). Four levels exist, but REST cannot reveal which; the app collapses to admin/own/none. Consider surfacing the level names in the no-access message text: ask admin to grant 'Call statistics' in Telephony > Access permissions. Recording listen is a SEPARATE permission from statistics — the playback spike must handle a user who sees rows but cannot listen (404/403 on CALL_RECORD_URL).
- [likely] (Portal administrators always have full telephony access; defaults) 'Bitrix24 administrators always have full access to Telephony sections regardless of the assigned role.' By default administrators hold the Administrator role and other users the Manager role (own calls only).
  - src: https://helpdesk.bitrix24.com/open/17441038/
  - impact: Supports the mapping user.admin=true → read all cached rows without probing. Default for a fresh portal: every non-admin employee sees own calls only, which matches 'PORTAL_USER_ID == own id'. Text came from search snippet of the official page; direct fetch of the page did not surface the sentence, so keep an eye on it.
- [likely] ('Call statistics' vs Manager role for call details) Search-result snippet from helpdesk: 'Call statistics. Employees will be able to view call details and statistics. Additionally, an employee needs to have at least the Manager role to view call details.'
  - src: https://helpdesk.bitrix24.com/open/18216960/
  - impact: No schema impact; confirms 'Call statistics' governs the call-details/statistics list that voximplant.statistic.get backs.
- [unverified] (Stability of user IDs and dismissed users in call statistics) User IDs are numeric primary keys referenced by PORTAL_USER_ID ('User identifier. The identifier can be obtained using the user.get method'); nothing in docs suggests renumbering. Documentation does not state whether calls of dismissed (ACTIVE=false) users remain in voximplant.statistic.get; there is no documented purge of statistics on dismissal.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Treat portal_user_id as a stable integer FK within a member_id. Assume historical rows for dismissed users persist (the safe assumption): employee cache must include inactive users, and the employee filter should show them (greyed) rather than hide them, otherwise counts will not reconcile. Verify empirically on a real portal during milestone 4.
- [verified] (Batch-fetching users) user.get supports '@ID' IN-array filtering, 50 rows per page, and ADMIN_MODE=true to read any users; general batch supports up to 50 commands (ERROR_BATCH_LENGTH_EXCEEDED documented). user.fields lists profile fields and varies by scope.
  - src: https://apidocs.bitrix24.ru/api-reference/user/user-get.html
  - impact: Employee sync job: collect distinct PORTAL_USER_ID from cached calls, chunk by 50, one batch call of up to 50 user.get commands (2,500 users per round trip). Alternatively full paging user.get with ADMIN_MODE=true for portals < a few thousand users. Store fetched_at and refresh with TTL (brief says short TTL; 1–6 h is adequate).
- [verified] (Method availability / scope list confirmation) Scope table lists user (versions user_brief minimal, user_basic basic), telephony, call (only infocall.startwithsound/startwithtext), placement, crm. Calling a method outside granted scopes returns insufficient_scope; methods needing app context return WRONG_AUTH_TYPE for webhooks.
  - src: https://apidocs.bitrix24.com/api-reference/scopes/permissions.html
  - impact: Error handler must distinguish insufficient_scope (config bug, fail loudly/log) from ACCESS_DENIED (user-rights state, render no-access UI) from expired_token (refresh once).

### Block (g) — CRM deals, funnels and stages (for the deal analytics page, §4.12)

Researched 2026-09-12 against apidocs.bitrix24.com and the b24restdocs source. Nothing in the
earlier blocks touched deals as *data*: block (c) established only that
`voximplant.statistic.get` has no `DEAL` entity type, which is a statement about telephony
rows, not about the CRM.

- [verified] (Method family) 'Development of the crm.deal.* methods has been discontinued. For new development, use the universal methods crm.item.* with entityTypeId = 2.' The camelCase mapping is documented on the same page: `STAGE_ID` -> `stageId`, `CATEGORY_ID` -> `categoryId`, `ASSIGNED_BY_ID` -> `assignedById`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/deals/index.html
  - impact: `crm.item.list` is the primary path; `crm.deal.list` is kept only as a `MethodNotFound` fallback for older on-premise builds.
- [verified] (OR logic) `filter` keys are AND-ed. Nested numeric-keyed groups carrying a `logic` member are documented ONLY for `crm.item.list`, with a worked example that unions two date ranges: `filter: {"0": {"logic": "OR", "0": {">=createdTime": ...}, "1": {">=createdTime": ...}}}`. No `logic` example exists on any `crm.deal.*` page.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/crm-item-list.html
  - impact: owner decision 3 (created OR modified OR closed) is ONE paged query on `crm.item.list` and three deduped selections on `crm.deal.list`. An undocumented `logic` on the old method would be UNSAFE: an ignored key returns a wrong, over-broad result rather than an error.
- [verified] (Page size) 'The page size for results is always static - 50 records.' No `limit` / `pageSize` / `top` parameter exists on the list contract; `start` must be a multiple of 50.
  - src: https://apidocs.bitrix24.com/api-reference/crm/deals/crm-deal-list.html
  - impact: request count is `ceil(N/50)` with no lever. This is THE cost variable of the no-storage design, and the reason for the preflight gate.
- [verified] (start: -1) Disables the total-count calculation. Measured on a 2,387,743-row selection: counting `next=50, total=2387743, duration=49.92s`; with `start=-1` `total=0`, no `next`, `duration=0.098s`. There is no `crm.*.count` method and no count-only flag anywhere.
  - src: https://apidocs.bitrix24.com/settings/performance/huge-data.html
  - impact: `start:-1` is a cursor tool, not a count tool. The deal report rejects it: forfeiting `total` forfeits the preflight gate, which is the only thing that can refuse an impossible report in two seconds instead of twenty-eight.
- [verified] (Envelope) `crm.deal.list` answers a BARE ARRAY in `result`; `crm.item.list` wraps its rows in `result.items`. Both carry `total`, `next` and `time` at the top level, and a batch reports them per command in `result_total` / `result_next` / `result_time`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/crm-item-list.html
  - impact: the row parser must branch on the method, not on the shape.
- [verified] (Dates accepted and returned) ISO 8601 with an explicit offset, e.g. `"DATE_CREATE": "2024-08-30T14:29:00+02:00"`. Filters accept the same form, and also a bare date. 'At the API level, all dates and times are stored according to the server parameters' — i.e. the PORTAL's zone, not the caller's.
  - src: https://apidocs.bitrix24.com/api-reference/crm/deals/crm-deal-get.html
  - impact: period bounds must be serialised WITH an offset. A bare date is read in the portal's zone and would shift the window for every viewer in another zone, which is the normal case for this app.
- [verified] (CLOSEDATE is not a close timestamp) `crm.deal.fields` declares `CLOSEDATE` as `{"type": "date", "isReadOnly": false}` — the deal's declared END of its date range, paired with `BEGINDATE`, pre-filled at creation and editable by anyone who can edit the deal. `DATE_CREATE` / `DATE_MODIFY` / `MOVED_TIME` are `datetime` and read-only.
  - src: https://apidocs.bitrix24.com/api-reference/crm/deals/crm-deal-fields.html
  - impact: the "closed in this period" leg uses the READ-ONLY pair `closed = "Y"` plus `movedTime`, never `CLOSEDATE`. A back-dated `CLOSEDATE` would pull years-old deals into a one-week report and a forward-dated one would hide a deal that closed yesterday.
- [verified] (Funnels) `crm.category.list {entityTypeId: 2}` is current and returns `result.categories[]` in camelCase (`id`, `name`, `sort`, `isDefault`), INCLUDING the default funnel as a real row with `id: 0`, `isDefault: "Y"`. `crm.dealcategory.list` is filed under OUTDATED and returns a flat UPPER_CASE array with no `isDefault`. Both are filtered by the CALLER's permissions.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/category/crm-category-list.html
  - impact: nothing needs synthesising for funnel 0. An EMPTY list means "this user may see no funnels", not "this portal has none" — it must render the `crm_no_access` state, never a blank table.
- [verified] (Stages) `crm.status.list {order: {SORT: "ASC"}, filter: {ENTITY_ID: ...}}`, where `ENTITY_ID` is `DEAL_STAGE` for the default funnel and `DEAL_STAGE_<id>` otherwise. Verbatim warning: 'the default funnel with the identifier 0 — the code without the numeric part, DEAL_STAGE. There is no DEAL_STAGE_0 code: with it, the method returns an EMPTY LIST WITHOUT AN ERROR.' Arrays are not accepted for `ENTITY_ID`, and each pipeline requires its own call.
  - src: https://apidocs.bitrix24.com/api-reference/crm/status/crm-status-list.html
  - impact: `DEAL_STAGE_0` is a SILENT bug — a funnel with zero columns, no exception and no log line. `tests/test_deal_aggregate.py` asserts both branches. Dictionary cost is `1 + K` commands for K funnels, packable into one batch.
- [verified] (SEMANTICS) A closed three-valued set on the stage directory: `null` = in progress, `"S"` = success, `"F"` = failure, declared `isImmutable`. Per-funnel invariant: exactly ONE `S` stage, possibly MANY `F` stages, the rest `null`. `crm.status.add` documents the in-progress value as `""` while every `crm.status.list` example returns JSON `null`. `EXTRA.SEMANTICS` is a different, open-ended text field (a published response carries `apology`).
  - src: https://apidocs.bitrix24.com/api-reference/crm/status/crm-status-fields.html
  - impact: normalise `null` and `""` identically, and accept only `S`/`F` — anything else is in-progress, so a value nobody has thought of is never counted as a won or a lost deal. "Refused" must sum over ALL `F` stages: a single lose-stage lookup under-counts every portal that added refusal reasons.
- [verified] (The deal carries its own semantic) `STAGE_SEMANTIC_ID` (`stageSemanticId`) is a read-only field on the deal itself with values `P` / `S` / `F`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/deals/crm-deal-fields.html
  - impact: the two rollup columns need no dictionary join at all. The dictionary is needed only for funnel names, column HEADERS and column ORDER.
- [verified] (Stage id format) `STATUS_ID` is category-prefixed for non-default funnels (`C1:NEW`, `C10:PREPAYMENT_INVOIC`) and bare in funnel 0 (`NEW`), and 'STATUS_ID uniqueness is limited to its own directory'. `NEW`, `WON` and `LOSE` come back `SYSTEM='Y'`; `PREPARATION`, `EXECUTING` and `APOLOGY` are `SYSTEM='N'` and may be deleted or renamed. `CATEGORY_ID` on a status row is a STRING for a numbered funnel and `null` for the default one.
  - src: https://apidocs.bitrix24.com/api-reference/crm/status/crm-status-list.html
  - impact: a column key must be the PAIR `(category_id, status_id)`; the `C<n>:` prefix is never parsed and never assumed present; and the category id is carried by the batch COMMAND KEY rather than read back off the row.
- [verified] (Stage names are data) `NAME` is a required, editable per-portal string, seeded in the portal's language at creation. `NAME_INIT` is read-only but populated only for `SYSTEM='Y'` stages. No `LANG` parameter exists on `crm.status.list` or `crm.category.list`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/status/crm-status-fields.html
  - impact: stage and funnel names pass through untranslated and must NEVER enter `web/messages/*.json` — `tools/check-i18n.mjs` would demand a matching key in both bundles for something no bundle can hold. Only the fallback for an unnameable stage is a catalogue key.
- [verified] (Batch cost) A batch is ONE request against the leaky bucket and is 'not accounted for in operating', but each nested method bills its own per-method-per-app operating time. Exceeding it returns HTTP 429 `OPERATION_TIME_LIMIT` and blocks THAT METHOD for THAT APP across the whole portal. Rate limits in 2026: drain 2/sec (Enterprise 5), burst 50 (Enterprise 250), 503 `QUERY_LIMIT_EXCEEDED` above it. A single cloud request is capped at 60 s.
  - src: https://apidocs.bitrix24.com/settings/performance/limits.html
  - impact: batching makes the report cheap on requests and NOT cheap on the thing that actually blocks a portal. Page batches are capped at 25 commands, not 50, and the endpoint reads `time.operating` adaptively. A runaway report would take `crm.item.list` away from this app portal-wide — including the CRM detail tab's own reads.
- [verified] (Scope) `crm.item.list`, `crm.category.list` and `crm.status.list` all require only the `crm` scope, which the app already holds. `crm.status.list` is executable by 'any user with permission to read at least one CRM object'; `crm.category.list` by any user, with the result filtered by access.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/crm-item-list.html
  - impact: no vendor-cabinet change and no forced re-consent for the whole feature.
- [unknown] (crm.status.list paging) The method reference documents only `order` and `filter` — no `start`, no page size — but the deal-funnels tutorial states 'API returns maximum 50 records per response'. Whether `start` is accepted here is not documented.
  - src: https://apidocs.bitrix24.com/api-reference/crm/status/crm-status-list.html
  - impact: a funnel with more than 50 stages may lose column HEADERS. The endpoint compares the row count against the returned `total` and reports the funnel in `scan.stage_dictionary_truncated`; the COUNTS stay correct because the missing stages are synthesised from the deals themselves.
- [unknown] (crm.category.list availability) No 'available from version X' note appears on the current docs, and the one web claim attributing it to CRM 21.400.0+ is not present on the page it cites.
  - src: not found
  - impact: never gate on a version number. Call it first and fall back on the typed `MethodNotFound` only; cache the verdict per portal.

### Corrections to brief (deal analytics)
- `crm.deal.list` cannot express the owner's "created OR modified OR closed" period in one query: its filter keys are strictly AND-ed and no `logic` grouping is documented for it. Only `crm.item.list` can, and that is why the universal method is the primary path rather than a modernisation for its own sake.
- `CLOSEDATE` is NOT "when the deal closed". It is a writable planned end date. Using it for the closed leg of a period would silently mis-file back-dated and future-dated deals.
- There is no count-only call. Every `total` costs a page of rows alongside it, and `start:-1` buys speed by removing `total` entirely.
- The camelCase spellings `updatedTime`, `movedTime`, `closed` and `stageSemanticId` are INFERRED from the documented mapping; only `createdTime` appears verbatim in a retrieved example. Because an unknown filter key may be ignored rather than refused, the endpoint proves both that the name exists (`crm.item.fields`) and that the filter is applied (a probe in the exact nested `logic` shape) before believing a portal.

### Open questions (deal analytics)
- Does `crm.status.list` accept `start`? If it does, a funnel with more than 50 stages could be read in full instead of reported as truncated.
- On an old on-premise build, does `crm.dealcategory.stage.list` really behave as the OUTDATED docs describe? The fallback dialect is only as good as its `FakeBitrix` script until a real such portal is seen.
- Is one deal always one order on the owner's portal? "Всего заказов" counts deals; summing `OPPORTUNITY` or a quantity user-field instead would be a different `select`, a different aggregation and a currency question.

### Block (h) — UTM tags, leads as a universal item, and the accounting currency (for §4.13)

Researched 2026-09-12 against apidocs.bitrix24.com. Block (g) covered deals as a pipeline;
nothing before this touched leads as DATA, and nothing touched UTM at all.

- [verified] (UTM fields exist on both entities) `utmSource`, `utmMedium`, `utmCampaign`, `utmContent`, `utmTerm` are listed among the COMMON fields of the universal CRM object model, for leads and for deals alike; the UPPER_CASE twins `UTM_SOURCE` … `UTM_TERM` are documented on `crm.lead.fields` and `crm.deal.fields` as plain strings with `isReadOnly: false`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/object-fields.html , https://apidocs.bitrix24.com/api-reference/crm/leads/crm-lead-fields.html , https://apidocs.bitrix24.com/api-reference/crm/deals/crm-deal-fields.html
  - impact: the feature is possible on the EXISTING `crm` scope. No vendor-cabinet change, no re-consent, no re-listing of the app.
- [verified] (They must be named explicitly) `select: ["*"]` returns standard fields only and excludes custom and multiple fields; the `UF_*` mask covers custom fields and there is no mask that covers UTM.
  - src: https://apidocs.bitrix24.com/api-reference/crm/leads/crm-lead-list.html
  - impact: `_select` names all five per dialect. An omitted `select` would return every `UF_*` too, which §6 forbids receiving, let alone logging.
- [verified] (A lead is entity type 1) `entityTypeId` is 1 for a lead and 2 for a deal in `crm.item.*`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/crm-item-list.html
- [verified] (The universal model has no `statusId`) The common field list documents `stageId` and `stageSemanticId` for leads and deals both, and lists no `statusId`: `crm.item.*` normalises a lead's `STATUS_ID` / `STATUS_SEMANTIC_ID` into the stage pair. The LEGACY methods keep the distinction — `crm.lead.list` says `STATUS_SEMANTIC_ID` where `crm.deal.list` says `STAGE_SEMANTIC_ID`.
  - src: https://apidocs.bitrix24.com/api-reference/crm/universal/object-fields.html
  - impact: one dialect SHAPE serves both entities on the universal path; only the legacy spellings diverge, which is why `bitrix/utm.py` is a table of four constants rather than one shape with an id swapped in.
- [verified] (Paging is the same as block (g)) Fixed 50 records per page, `start` offset, `total` in the envelope.
  - src: https://apidocs.bitrix24.com/api-reference/crm/leads/crm-lead-list.html
  - impact: the preflight `total` bounds the cost of BOTH legs, which is what lets §4.13 refuse an impossible selection in one round trip rather than discovering it at the browser's timeout.
- [unknown] (Does `select` actually RETURN the UTM fields?) `crm.item.fields` proves a field exists on the entity; no retrieved page states that `crm.item.list` returns `utmSource` when it is named in `select`. Bitrix24 omits null keys rather than returning `null`, so an all-empty result is indistinguishable from "these records genuinely carry no tags".
  - impact: reported rather than detected — `scan.<entity>.tagged_rows` plus one sentence on the page that is true in both worlds. **This is the highest-value thing to settle on a real portal:** `crm.item.list {entityTypeId:1, select:["id","utmSource"], filter:{"=id":<a tagged lead>}}`. A positive answer deletes a hedge; a negative one makes the legacy dialects the only path and `LEAD_ITEM`/`DEAL_ITEM` dead code.
- [unknown] (`OPPORTUNITY_ACCOUNT` / `ACCOUNT_CURRENCY_ID`) Neither appears in the retrieved lead or deal field tables; the pair is documented only in the SPA section. Whether it exists on leads and deals, and whether it is read-only, is unconfirmed.
  - impact: §4.13 probes for it and falls back to `OPPORTUNITY` + `CURRENCY_ID` grouped by currency. Either way the singleton guard decides whether a money column is shown at all, so a wrong guess costs a hidden column and never a wrong number.
- [unknown] (Does lead→deal conversion copy `UTM_*`?) Believed yes — the fields exist on both entities and the converter carries them — but not verified against a retrieved page or a live portal.
  - impact: if it does NOT, §4.13's independent grouping is weaker and the attribution note becomes the page's most important sentence rather than a caveat. The decision not to join on `leadId` stands either way, because the join's cost is unbounded at preflight.
- [unknown] (What does `crm.item.list {entityTypeId:1}` answer in simple CRM mode?) It may error, or it may return an empty list. If it returns empty, "leads are turned off" and "no leads this month" are indistinguishable and the copy must cover both.
  - impact: §4.13 treats only a typed `MethodNotFound` / `AccessDenied` as "unavailable", so an empty 200 currently reads as "no leads this month".


### Corrections to brief
- Brief says telephony roles are 'Administrator/Manager/Operator'. Official helpdesk lists default roles Administrator, Chief executive, Head of department, Manager; there is no 'Operator' default role. The 'Call statistics' permission has four values: Only their own calls / Calls from their department / Any calls / No access.
- Brief implies the 'no rights' case is detected by 'render explicit state'. Concretely: voximplant.statistic.get with the user's token returns error code ACCESS_DENIED ('Insufficient permissions to view call statistics'), not an empty result; a user with 'own calls' level gets a normal (filtered) 200 response. The app cannot distinguish 'department' from 'own' or 'any' for non-admins via REST — only admin vs non-admin vs denied.
- Brief says 'check user.admin' as if it needs a scope: user.admin (and profile) are in the implicit 'basic' scope, callable by any user with no scope grant; user.current requires user_brief (granted). user.admin semantically means 'can manage application settings', which apidocs equates with Bitrix24 administrator rights via profile.ADMIN / BX24.isAdmin.
- Brief says 'Employee names come from user_brief; cache them per portal with a short TTL' — correct, but note the employee cache must NOT filter ACTIVE=true (default docs wording: ACTIVE=true excludes terminated users), because historical calls reference dismissed users.
- Recording listening is a separate telephony permission ('Call Recording: Listen') from 'Call statistics'; a user may see rows but be unable to play recordings. The playback spike must model this state.

### Open questions
- Does voximplant.statistic.get keep rows for dismissed/deleted users (PORTAL_USER_ID of an ACTIVE=false or removed user)? Not documented; verify on a real portal during milestone 4.
- Does ADMIN_MODE=true on user.get require the calling user to be a portal admin, and does it change field visibility under user_brief? Docs give no executor restriction; test with the install (admin) token and a non-admin token.
- When a non-admin user calls voximplant.statistic.get with FILTER PORTAL_USER_ID of another employee while having 'department' or 'any' level, does the server honour it? Relevant only if the app later wants department-level views; v1 ignores.
- Does the install-time admin token remain valid for sync if that administrator is later dismissed or demoted? (Likely token invalidation — plan for a 'reinstall required' portal state.)
- Exact HTTP status returned with ACCESS_DENIED for statistic.get (docs list ACCESS_DENIED without status; system table shows 403 for INVALID_CREDENTIALS). Match on error code string, not status.

## Research block
- [verified] ((a) Query-string params on app open) Bitrix24 opens the app handler with query params DOMAIN ("The address of the Bitrix24 where the application is open"), PROTOCOL (0=HTTP, 1=HTTPS), LANG (interface language of the user who opened the app), APP_SID ("application session identifier. Bitrix24 generates a new one each time").
  - src: https://apidocs.bitrix24.com/settings/oauth/simple-way.html
  - impact: APP_SID is per-render and useless as a session key; never persist it. DOMAIN is display/routing info only, not a tenant key. Build client_endpoint as (PROTOCOL?https:http)://DOMAIN/rest/ only as a fallback; prefer client_endpoint returned by the token server.
- [verified] ((a) POST body params on app open) POST body: AUTH_ID ("same as access_token"), AUTH_EXPIRES (token lifetime in seconds, e.g. 3600), REFRESH_ID (same as refresh_token), SERVER_ENDPOINT ("address of the authorization server that the application contacts for a new pair of tokens"), APPLICATION_TOKEN ("handler can use it to verify that the request came from Bitrix24"; same value as application_token in event handlers), APPLICATION_SCOPE (comma-separated granted scopes), member_id ("unique identifier of Bitrix24, independent of the domain name"), status, PLACEMENT ("For the main application page it is DEFAULT"), PLACEMENT_OPTIONS (JSON string with context, e.g. ID and URI for widgets).
  - src: https://apidocs.bitrix24.com/api-reference/widgets/contact-center.html
  - impact: Handler endpoint must accept application/x-www-form-urlencoded POST. Persist APPLICATION_SCOPE per portal to detect scope drift. Every open delivers a fresh user access token (AUTH_ID) — use it for per-request authorisation instead of the stored admin token. AUTH_EXPIRES is a duration, not an epoch: compute expires_at = now + AUTH_EXPIRES.
- [verified] ((a) status values) status values documented: L — local application, F — free mass-market application, D — demo version of a mass-market application, T — trial version (time-limited), P — paid mass-market application. The Russian docs additionally list S — subscription mass-market application (подписное тиражное приложение); app.info RU also lists S (Subscription). The English pages do not list S.
  - src: https://apidocs.bitrix24.ru/settings/oauth/simple-way.html
  - impact: Store status as a free-form 1-char string; do not use a strict enum that rejects unknown values (S exists, more may come). App is Free so expect F; if P/T/D/S ever appears, treat as valid.
- [verified] ((a) Install page receives same params) The installation script receives the same data as a regular application page, with PLACEMENT=DEFAULT for the main installation page (RU simple-way page). Install wizard page URL is configured in the vendor cabinet field 'Path for initial installation'/'Installation application URL'; the app must call BX24.installFinish() at the end, after registering placements and subscribing to events.
  - src: https://apidocs.bitrix24.com/settings/app-installation/installation-finish.html
  - impact: /install/ must parse the same POST body as /app/. Do placement.bind server-side with AUTH_ID before returning the HTML that calls BX24.installFinish(); if placement.bind fails, still render installFinish but log — otherwise the app stays 'not installed'.
- [verified] ((a) Until installFinish, app is not installed) "Until installFinish is called, an application with an interface is considered not installed, and Bitrix24 does not deliver events to it." Widgets do not appear even if placement.bind succeeded; regular users see 'The application is not fully installed yet'; app.info INSTALLED=false means installFinish was never executed.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-install-finish.html
  - impact: Add a portal.installed_at / install_completed flag; the /app/ handler should call app.info and, if INSTALLED=false for an admin, re-render the install page instead of the dashboard. ONAPPINSTALL/ONAPPUNINSTALL will not arrive before installFinish.
- [verified] ((b) Refresh endpoint request) Refresh is a GET to https://oauth.bitrix.info/oauth/token/ with grant_type=refresh_token, client_id (app code), client_secret (server-side only), refresh_token.
  - src: https://apidocs.bitrix24.com/settings/oauth/auto-renewal.html
  - impact: httpx GET with query params; client_secret from env. Log request without the secret/tokens.
- [verified] ((b) Refresh response JSON) Documented sample response: {"access_token","refresh_token","expires":1780319382 (epoch),"expires_in":3600,"scope":"crm,entity,im,task","domain":"oauth.bitrix.info","server_endpoint":"https://oauth.bitrix.info/rest/","client_endpoint":"https://portal.bitrix24.com/rest/","member_id":"a223c6b3710f85df22e9377d6c4f7553","user_id":67,"status":"F"}. Note: 'domain' in the token response is the authorization server, not the portal ("distinguishes authorization server from Bitrix24 address").
  - src: https://apidocs.bitrix24.com/settings/oauth/auto-renewal.html
  - impact: Do NOT overwrite the stored portal domain with response.domain (it is oauth.bitrix.info). Update client_endpoint, member_id sanity-check (must equal stored), user_id (owner of the token), scope, status, expires (epoch) from the response. Store both tokens.
- [verified] ((b) Refresh token rotates; lifetimes) The refresh response contains "a new value of the renewal token. Retain it instead of the previous one." access_token lifetime is one hour; refresh_token lifetime is 180 days. Docs warn: "Do not renew the token before every REST API request, and do not schedule the renewal for once an hour or once a day" — excessive refreshes can get the app blocked.
  - src: https://apidocs.bitrix24.com/settings/oauth/auto-renewal.html
  - impact: Refresh only on expired_token (or when stored expires is past) and serialize refreshes per portal (row lock / advisory lock) so concurrent sync jobs never race and burn the single-use refresh token. If the portal is idle >180 days the stored refresh token dies; recover on next admin open (fresh AUTH_ID/REFRESH_ID POSTed). Store refresh_token_updated_at.
- [verified] ((b) Sync worker must use tokens obtained by refresh, keyed by member_id) Refresh works for both cloud and self-hosted only when calling oauth.bitrix.info directly; in cloud the portal's own /oauth/token/ is proxied to the auth server but "Bitrix24 Self-Hosted may not do this."
  - src: https://apidocs.bitrix24.com/settings/cloud-and-on-premise/on-premise/index.html
  - impact: Hardcode the refresh URL to https://oauth.bitrix.info/oauth/token/ (or use SERVER_ENDPOINT host) — never build it from DOMAIN. Note SERVER_ENDPOINT may be a different host in other regions (RU docs show https://oauth.bitrix24.tech/rest/), so prefer the SERVER_ENDPOINT/server_endpoint value the portal sends, replacing /rest/ with /oauth/token/, with oauth.bitrix.info as default.
- [verified] ((c) On-premise: REST endpoint and OAuth) For self-hosted Bitrix24 with Marketplace apps, REST calls go to client_endpoint (https://<portal-domain>/rest/) exactly as in cloud; token refresh must go to oauth.bitrix.info; the box needs network access to oauth.bitrix.info ("If network access to it is closed, REST will not work in a self-hosted Bitrix24 instance"). Events "pass through an event queue located in the cloud, even when dealing with a self-hosted Bitrix24 instance." REST version on box "may lag behind the Cloud version", so verify method availability (methods/events) at install.
  - src: https://apidocs.bitrix24.com/settings/cloud-and-on-premise/on-premise/index.html
  - impact: Store client_endpoint per portal and use it for all REST; DOMAIN may be any hostname (not *.bitrix24.*), possibly with non-standard port/HTTP (PROTOCOL=0). At install, call 'methods'/'method.get' to confirm voximplant.statistic.get exists and record a capability flag; show a clear state if missing.
- [verified] ((c) Isolated box (custom auth provider)) Fully isolated boxes can plug a custom authorization/event provider that excludes oauth.bitrix.info: tokens are generated locally, events are POSTed directly by the box, and the example shows REFRESH_ID empty; status is reported as local application.
  - src: https://apidocs.bitrix24.com/settings/cloud-and-on-premise/on-premise/custom-auth-provider.html
  - impact: Do not assume REFRESH_ID is non-empty. If refresh token is absent, mark portal as 'refresh-unavailable' and rely on tokens delivered on each iframe open (sync only works while an admin opens the app). Out of scope for v1 but the schema should allow NULL refresh_token.
- [verified] ((d) ONAPPINSTALL delivery for mass-market apps) ONAPPINSTALL handler "can be set in the installation script of the application, which is specified in the version card in a separate field." The mass-market 'installation callback' page: the URL is set in the application card field 'Event installation handler URL' (HTTPS required); Bitrix24 POSTs to it ONAPPUSERREADY (when the app system user is created/re-activated; system-user tokens in data, installer's tokens in auth) and ONAPPINSTALL ("if the application works only through the API, without its own page"). Installation for a no-interface app does not depend on the handler's response; return HTTP 200. Events are not sent until installation is complete.
  - src: https://apidocs.bitrix24.com/settings/app-installation/mass-market-apps/installation-callback.html
  - impact: For our app WITH an interface, do not rely on ONAPPINSTALL arriving; the authoritative install path is the /install/ iframe POST (AUTH_ID/REFRESH_ID/APPLICATION_TOKEN) + installFinish. Still expose one /events/ endpoint (set in the vendor cabinet) that accepts ONAPPINSTALL, ONAPPUSERREADY, ONAPPUPDATE, ONAPPUNINSTALL and upserts tokens/application_token keyed by member_id. Additionally call event.bind('ONAPPUNINSTALL', handler) from /install/ as belt-and-braces (event.bind requires admin/user auth; not confirmed on docs for lifecycle events — see open questions).
- [verified] ((d) ONAPPINSTALL payload) POST form-urlencoded: event=ONAPPINSTALL; data[VERSION], data[ACTIVE]=Y/N, data[INSTALLED]=Y/N, data[LANGUAGE_ID]; ts; auth[domain], auth[scope] (space-separated in event payloads per docs), auth[access_token], auth[refresh_token], auth[expires_in]=3600, auth[server_endpoint], auth[status], auth[client_endpoint], auth[member_id], auth[application_token]. Callback example also includes event_handler_id. No data[CLEAN] on install.
  - src: https://apidocs.bitrix24.com/api-reference/common/events/on-app-install.html
  - impact: Parse PHP-style bracketed form keys (auth[member_id]) — use a small parser or python's urllib.parse.parse_qs + bracket expansion. Store auth.user_id if present (not in the example) — treat as optional. Scope separator differs (comma on open/refresh vs space in events): normalise by splitting on [ ,].
- [verified] ((d) ONAPPUNINSTALL payload) POST: event=ONAPPUNINSTALL; data[LANGUAGE_ID], data[CLEAN] (1 or 0 — "Value of the 'Clear application data' option set by the user during uninstallation"); ts; auth[domain], auth[server_endpoint], auth[client_endpoint], auth[member_id], auth[application_token]. No access_token: "When an application is uninstalled, all access permissions for the application to the API are revoked."
  - src: https://apidocs.bitrix24.com/api-reference/common/events/on-app-uninstall.html
  - impact: Uninstall handler must not call REST (placement.unbind will fail with revoked auth — do not make the brief's 'placement.unbind on uninstall' a hard step; placements die with the app anyway). Verify application_token, mark portal inactive, wipe tokens; honour CLEAN=1 by deleting cached rows (brief already deletes rows — fine either way).
- [verified] ((d) application_token verification) Official rule: retrieve the stored application_token by auth.member_id, compare with incoming auth.application_token, return HTTP 403 on mismatch; "especially important" for ONAPPUNINSTALL since no auth is passed. Treat application_token as a secret (no logs, no client-side). It is first passed at ONAPPINSTALL (or APPLICATION_TOKEN on app open) and CHANGES when a new app version is installed (ONAPPUPDATE carries the updated application_token — update the stored value).
  - src: https://apidocs.bitrix24.com/api-reference/events/safe-event-handlers.html
  - impact: Store application_token encrypted per portal; handle ONAPPUPDATE by replacing it; also refresh the stored value from APPLICATION_TOKEN on every /app/ open (it is the same value) so a version bump does not lock out later events. Constant-time compare. Optionally also validate source IPs against https://dl.bitrix24.com/webhook/app-world.json (poll at most every 5-10 min).
- [verified] ((d) ONAPPUPDATE) ONAPPUPDATE data: VERSION, PREVIOUS_VERSION, LANGUAGE_ID; auth carries full tokens (access_token, refresh_token, expires_in, scope, server_endpoint, client_endpoint, member_id, status) and the updated application_token.
  - src: https://apidocs.bitrix24.com/api-reference/common/events/on-app-update.html
  - impact: Events endpoint must handle ONAPPUPDATE: update application_token and (optionally) tokens; store app_version per portal.
- [verified] ((d) Application system user / ONAPPUSERREADY) Bitrix24 provides an application 'system user' (technical account, not in employee list, excluded from user.get/user.search) so integrations survive employee termination. Apps receive its long-lived authorization in the ONAPPUSERREADY event via the installation callback. For applications it "receives the role of a regular employee" and does not inherit elevated roles of the installing admin. Not supported for local apps.
  - src: https://apidocs.bitrix24.com/settings/system-user.html
  - impact: Do NOT use the system-user token for the call-statistics sync: as a regular employee it likely lacks 'Call statistics — view' for all users. Keep the brief's design (installing admin's token for sync), but store ONAPPUSERREADY tokens separately (portal_tokens.kind = installer|system_user) for future use. Handle the case where the installing admin is fired: sync breaks with auth errors — detect and prompt an admin to re-open the app to re-seed tokens.
- [verified] ((e) REST auth error codes) Official error list: 401 NO_AUTH_FOUND 'Wrong authorization data'; 401 expired_token 'The access token provided has expired'; 403 ACCESS_DENIED 'REST API is available only on commercial plans'; 403 INVALID_CREDENTIALS 'Invalid request credentials' (user lacks permission); 403 insufficient_scope; 403 user_access_error 'The user does not have access to the application'; 400 INVALID_REQUEST 'Https required'; 500 PORTAL_DELETED 'Portal was deleted'; 503 QUERY_LIMIT_EXCEEDED 'Too many requests'; 503 OVERLOAD_LIMIT; 404 ERROR_MANIFEST_IS_NOT_AVAILABLE; 400 ERROR_BATCH_LENGTH_EXCEEDED; 405 ERROR_BATCH_METHOD_NOT_ALLOWED; 500 INTERNAL_SERVER_ERROR / ERROR_UNEXPECTED_ANSWER. Error body shape: {"error":"CODE","error_description":"text"}. OAuth-server errors: invalid_request, invalid_client, insufficient_scope/invalid_scope, invalid_grant, PAYMENT_REQUIRED ({"error":"PAYMENT_REQUIRED","error_description":"Payment required"}). invalid_token and WRONG_AUTH_TYPE are NOT on the current official pages (WRONG_AUTH_TYPE only appears in a search snippet: 'Current authorization type is denied for this method').
  - src: https://apidocs.bitrix24.com/error-codes.html
  - impact: Match error codes case-insensitively (docs mix cases). Refresh-and-retry only on expired_token; on NO_AUTH_FOUND/invalid_grant mark tokens invalid and require an admin re-open; on PORTAL_DELETED mark portal inactive; on user_access_error/INVALID_CREDENTIALS render the permission state; on ACCESS_DENIED (non-commercial plan) render 'plan does not include REST'; on QUERY_LIMIT_EXCEEDED/OVERLOAD_LIMIT back off (503).
- [verified] ((f) app.info) app.info (scope basic) returns ID, CODE, VERSION, STATUS (F/D/T/P/L; RU docs add S), INSTALLED (bool — false means installFinish not executed; events/placements withheld), PAYMENT_EXPIRED (Y/N), DAYS (days left, null for free), LANGUAGE_ID, LICENSE (e.g. de_ent10000), LICENSE_TYPE, LICENSE_FAMILY. LICENSE_PREVIOUS is not documented.
  - src: https://apidocs.bitrix24.com/api-reference/common/system/app-info.html
  - impact: Call app.info at install and periodically in sync: persist INSTALLED, VERSION, LICENSE_FAMILY, LANGUAGE_ID per portal. Use LICENSE region prefix (kz_…) for diagnostics; do not gate features on it.
- [likely] ((g) member_id stability and DOMAIN change) member_id is "the unique identifier of Bitrix24, independent of the domain name". Bitrix24 cloud addresses can be changed by an administrator (once on any plan; further changes via Support on Professional+), and custom domains can be connected; helpdesk says after a change you must reconnect Open Channels, reinstall site widgets, recreate webhooks — installed Marketplace apps are not listed as needing reinstall. There is no official statement that member_id changes on rename.
  - src: https://apidocs.bitrix24.com/settings/oauth/simple-way.html
  - impact: Primary key = member_id (32-hex string, UNIQUE). DOMAIN and client_endpoint are mutable attributes: update them on every /app/ open and every token refresh; never use domain as a lookup key or cache-key component. Keep a domain history/log for audit.
- [verified] ((d) Event source network) Events are delivered from a dynamic pool of Bitrix24 servers; the current IP list is at https://dl.bitrix24.com/webhook/app-world.json (poll at most once per minute, ideally every 5-10 min). Handler URLs must be publicly reachable over HTTPS.
  - src: https://apidocs.bitrix24.com/settings/cloud-and-on-premise/network-access.html
  - impact: Optional defence-in-depth IP allowlist at Caddy or in the events handler; primary check remains application_token.
- [verified] ((a) BX24.getAuth in frontend) BX24.getAuth() returns {access_token, refresh_token, expires_in, domain, member_id}; returns false before init or after expiry.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-get-auth.html
  - impact: Frontend can forward a fresh AUTH_ID to the backend for per-request authorisation if the original POST token expired inside a long-lived iframe session; backend must verify it via user.current before trusting it.

### Corrections to brief
- Brief item 7 says 'On uninstall: placement.unbind'. Official ONAPPUNINSTALL docs: all API access is revoked at uninstall and the event carries no access_token, so placement.unbind cannot be executed from the uninstall handler. Drop it from the uninstall path (placements are removed with the app); keep 'mark inactive + delete cached rows'.
- Brief assumes ONAPPINSTALL is delivered to a Marketplace app with a UI. Official installation-callback page states ONAPPINSTALL goes to the 'Event installation handler URL' and is sent when 'the application works only through the API, without its own page'; ONAPPUSERREADY is sent instead when the app system user is created. Treat the /install/ iframe POST (AUTH_ID, REFRESH_ID, APPLICATION_TOKEN, member_id) as the authoritative install source and treat ONAPPINSTALL as optional/idempotent.
- Brief lists status values F, D, T, P, L, S. English docs list only L, F, D, T, P; the S (subscription) value exists only in the Russian docs (simple-way and app.info RU). Do not enumerate strictly.
- Brief item 3: refresh response 'domain' field is the authorization server (oauth.bitrix.info), not the portal domain — do not overwrite the stored portal domain from it. Response also contains 'expires' (epoch) and 'user_id', useful for expiry tracking and identifying the token owner.
- Brief item 5 mentions verifying only ONAPPINSTALL/ONAPPUNINSTALL by APPLICATION_TOKEN. Official docs say application_token changes on version update and is delivered via ONAPPUPDATE; the events endpoint must also handle ONAPPUPDATE (and ONAPPUSERREADY) and replace the stored token, otherwise a version bump will cause later uninstall events to be rejected.
- Brief item 3 says only that the refresh returns a new refresh token; docs additionally warn that refreshing on a schedule (hourly/daily) or before every request can get the app blocked — refresh only on expired_token or known expiry, serialized per portal.
- Brief 'Permission model' says the sync uses the installing administrator's token. Docs add that if that employee is dismissed, the app may be transferred to an 'application system user' with regular-employee rights (ONAPPUSERREADY). The schema should distinguish token owner (user_id) and kind, and the sync must detect loss of statistics rights rather than assume the install token stays admin-level forever.
- Brief item 5 lists error 'invalid_token' and 'WRONG_AUTH_TYPE'. Neither appears on the current official error-code pages; the documented 401 codes are NO_AUTH_FOUND and expired_token, plus 403 INVALID_CREDENTIALS, user_access_error, insufficient_scope. Handle codes case-insensitively and keep an 'unknown auth error' path.

### Open questions
- Whether ONAPPINSTALL is also sent for a mass-market app that has an interface (the callback page says API-only apps get it; the ONAPPINSTALL page does not restrict it). Confirm empirically with a test portal; design must work either way.
- Whether event.bind can subscribe to ONAPPUNINSTALL/ONAPPINSTALL (docs do not confirm or deny); test in the install flow and fall back to the vendor-cabinet 'Event installation handler URL'.
- Whether member_id truly survives a portal address change / custom-domain connection — official docs say it is domain-independent but do not explicitly address renames; verify with Bitrix24 support or a test rename.
- Exact JSON body of expired_token for a batch call (does batch return HTTP 200 with a per-command error, or HTTP 401 overall?) — affects the refresh-and-retry wrapper.
- Whether SERVER_ENDPOINT for the kz region is oauth.bitrix.info or a regional host (RU docs show oauth.bitrix24.tech); use the value the portal sends.
- Whether the application system user (ONAPPUSERREADY) receives 'Call statistics — view' rights on any plan; if so the sync could use it instead of the admin token.

## Research block
- [verified] ((a) permission / scope) voximplant.statistic.get requires scope `telephony` and 'Who can execute the method: user with Call Statistics — View permission'. Without it the method returns HTTP 403 with error code ACCESS_DENIED ('Access denied!' / 'Insufficient permissions to view call statistics'). Other listed errors: 401 NO_AUTH_FOUND, 503 QUERY_LIMIT_EXCEEDED, 429 OPERATION_TIME_LIMIT, 500 INTERNAL_SERVER_ERROR.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Sync worker must treat ACCESS_DENIED on the portal (installer) token as a terminal, user-visible portal state ('installer lost Call statistics — view'), not a retryable error. The per-user access probe for the UI can be a 1-row call with the current user's AUTH_ID; 403/ACCESS_DENIED => render the 'ask your administrator' state.
- [verified] ((a) permission — product-level levels) Bitrix24 Telephony access permissions (CRM > Add-ons > Telephony > Access permissions) define roles Administrator / Chief executive / Head of department / Manager; the 'Call statistics' permission has four values: only their own calls, calls from their department, any calls, no access. Separate permissions exist for 'listen to call recordings' (own / department / any / none) and 'delete recordings'. By default only account administrators have access to call statistics.
  - src: https://helpdesk.bitrix24.com/open/18216960/
  - impact: The brief's binary model (portal admin sees all, everyone else sees only PORTAL_USER_ID == own) is narrower than Bitrix24's own model (department level exists; recording-listen right is separate from statistics-view right). At minimum document this as a v1 simplification; ideally derive visibility by querying voximplant.statistic.get live with the user's token instead of guessing. The docs do NOT say whether the REST method silently filters rows by 'own/department' level or only gates on 'no access'.
- [verified] ((a) returned fields) Documented result fields: ID (integer, internal statistics record id), CALL_ID (string), EXTERNAL_CALL_ID (string, id on the external PBX side, may be null), CALL_CATEGORY (string, e.g. 'external'), PORTAL_USER_ID (integer), PORTAL_NUMBER (string), PHONE_NUMBER (string), CALL_TYPE (integer 1 outgoing, 2 incoming, 3 incoming with redirection, 4 callback, 5 informational), CALL_DURATION (integer seconds), CALL_START_DATE (datetime ISO-8601 with tz), CALL_LOG (string URL to voximplant log, may be null), CALL_RECORD_URL (string URL or '' / null), CALL_VOTE (integer 1-5, 0 or null if absent), COST (double), COST_CURRENCY (string), CALL_FAILED_CODE (string), CALL_FAILED_REASON (string), CRM_ENTITY_TYPE (string), CRM_ENTITY_ID (integer), CRM_ACTIVITY_ID (integer), REST_APP_ID (integer), REST_APP_NAME (string), TRANSCRIPT_ID (integer), TRANSCRIPT_PENDING ('Y'/'N'), SESSION_ID (integer, 'Session identifier on the telephony side'), REDIAL_ATTEMPT (integer), COMMENT (string), RECORD_DURATION (integer, seconds), RECORD_FILE_ID (integer or null).
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Add CALL_CATEGORY, CALL_LOG, TRANSCRIPT_ID, TRANSCRIPT_PENDING to the persisted column list (cheap, useful for 'line / source' and future features). Make EXTERNAL_CALL_ID, SESSION_ID, CALL_RECORD_URL, RECORD_FILE_ID, RECORD_DURATION, CRM_* all nullable. Unique key per portal should be (member_id, ID).
- [verified] ((a) JSON value types in practice) In the official response example numeric fields are serialized as strings: "ID": "1", "CALL_TYPE": "1", "CALL_DURATION": "0", "CALL_FAILED_CODE": "200", "CALL_VOTE": "5", "COST": "0.0000", "CRM_ENTITY_ID": "275", "SESSION_ID": "3841557776", while RECORD_FILE_ID is a bare integer (9079) or null and EXTERNAL_CALL_ID / CALL_LOG / CALL_RECORD_URL may be null.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Parser (pydantic) must coerce strings to int/Decimal and accept null/'' interchangeably. Store COST as NUMERIC(12,4), not float. Store CALL_FAILED_CODE as VARCHAR (values like '603-S' and 'OTHER' exist).
- [verified] ((a) CALL_START_DATE format & timezone) CALL_START_DATE is ISO-8601 with timezone offset, e.g. "2025-08-06T14:08:40+03:00" (RU page) / "+01:00" (EN page). The general data-types page says: the UI adapts dates to each user's timezone, but 'at the API level, all dates and times are stored according to the server parameters', i.e. the offset reflects the portal/server timezone, not the caller's.
  - src: https://apidocs.bitrix24.com/api-reference/data-types.html
  - impact: Store CALL_START_DATE as TIMESTAMPTZ (parse the offset, keep UTC). For 'today / per day / hour x weekday' aggregation, convert to the portal's timezone (take it from the offset seen in rows or from a portal setting), never the server's local time. Filter values must be sent in the same ISO-8601-with-offset form, e.g. ">=CALL_START_DATE": "2025-01-01T00:00:00+01:00".
- [verified] ((a) CALL_FAILED_CODE values) Documented CALL_FAILED_CODE values: 200 success, 304 missed, 603 declined, 603-S cancelled (declined by caller), 403 forbidden, 404 invalid number, 486 busy, 484 / 503 unavailable (unreachable/service unavailable), 480 temporarily unavailable, 402 insufficient funds, 423 blocked, OTHER undefined.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: 'Result' filter/summary needs a mapping table beyond the brief's four codes; treat 'answered' = 200, 'missed' = 304, and group the rest as 'not connected' with the raw code shown. Keep the column as string.
- [verified] ((a) CRM_ENTITY_TYPE values) CRM_ENTITY_TYPE is documented as one of CONTACT, COMPANY, LEAD only (same set in telephony.externalCall.register). DEAL is not a documented value; deals are reached through CRM_ACTIVITY_ID bindings.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: The CRM_DEAL_DETAIL_TAB placement cannot be served by filtering cached rows on CRM_ENTITY_TYPE='DEAL'. For deals you must resolve the deal's contact/company (crm.deal.get -> CONTACT_ID/COMPANY_ID, or crm.activity.list by OWNER_TYPE_ID=2/OWNER_ID and match CRM_ACTIVITY_ID) and show calls linked to those. Plan this in the schema (index on CRM_ACTIVITY_ID) and in the placement handler.
- [verified] ((b) FILTER operators) FILTER supports operators as prefix on the field name: = (default), !=/!, >, >=, <, <=, >< (inclusive range, value is an array), !>< (outside range), ? (string search), % (LIKE), !% (NOT LIKE). Array value for equality means IN, e.g. "ID": [1, 7]. Example: {"ID": [1,7], ">=CALL_START_DATE": "2025-01-01T00:00:00+01:00"}. Filterable fields = the full field list above (PORTAL_USER_ID, CALL_TYPE, CRM_ENTITY_TYPE, CRM_ENTITY_ID, CRM_ACTIVITY_ID, CALL_START_DATE, ID, etc.).
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Incremental sync can use ">ID": last_id (monotonic internal id) instead of CALL_START_DATE; backfill can use "><CALL_START_DATE": [from, to] windows. Filtering by PORTAL_USER_ID/CRM_ENTITY_* is possible server-side, useful for the per-user access probe and for the CRM tab if you ever query live.
- [verified] ((b) SORT / ORDER / pagination) SORT: string, any filter field except CALL_TYPE. ORDER: ASC or DESC. start: integer offset, page size fixed at 50, start = (N-1)*50. Response carries result[], total (count matching filter), next (offset of the next page, present only if more), and time{}.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Backfill: SORT=ID, ORDER=ASC, then page by 'next' until absent; better, use cursor style (">ID": last_seen_id, start=0) to avoid offset drift while new calls arrive. 'total' lets the UI show backfill progress. No page-size parameter exists; 50 is fixed.
- [verified] ((c) admin check) user.admin (scope basic, any user can call) returns true/false = whether the current authenticated user 'has permissions to manage application settings'; it evaluates the user behind the token used.
  - src: https://apidocs.bitrix24.com/api-reference/common/users/user-admin.html
  - impact: Use user.admin with the request's AUTH_ID for the 'portal admin' branch; cache per (member_id, user_id) with short TTL. Note the doc phrases it as application-settings management, which in cloud Bitrix24 corresponds to portal administrators; do not rely on it for the Bitrix24 telephony role.
- [likely] ((d) CALL_RECORD_URL / RECORD_FILE_ID) Official examples show CALL_RECORD_URL as "" or null even in a row where RECORD_FILE_ID = 9079 and TRANSCRIPT_ID = "1"; docs describe CALL_RECORD_URL only as 'URL of the call recording' and give no sample URL, lifetime, signing or session requirement. RECORD_FILE_ID has no textual description; the same value 9079 appears as FILE_ID returned by telephony.externalCall.attachRecord, and recordings are stored in Company Drive folder 'Telephony – recorded calls' (helpdesk), so RECORD_FILE_ID is a Drive file id usable with disk.file.get.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-statistic-get.html
  - impact: Do not assume CALL_RECORD_URL is populated whenever a recording exists; 'has recording' must be RECORD_FILE_ID IS NOT NULL OR CALL_RECORD_URL <> ''. The playback spike must test the actual CALL_RECORD_URL string on a real portal (format, expiry, cookies). Without the disk scope, disk.file.get is unavailable (403 insufficient_scope), so option (3) in the brief is truly blocked unless the scope is added.
- [verified] ((d) disk.file.get DOWNLOAD_URL) disk.file.get requires scope disk and Read access to the file for the calling user. It returns DOWNLOAD_URL = 'Link to download the file' of the form https://<portal>/rest/download.json?auth=<access_token>&token=disk%7C..., i.e. a link that embeds the caller's REST access token plus a signed disk token; DETAIL_URL opens the file in the UI. The docs state no lifetime, but the embedded auth token expires with the access token (1h).
  - src: https://apidocs.bitrix24.com/api-reference/disk/file/disk-file-get.html
  - impact: If the disk scope is ever added: never hand DOWNLOAD_URL to the browser (it leaks the portal access token); proxy it from the backend with the portal token, honour Range headers, and re-fetch the URL after a token refresh. Also 'listen to recordings' is a separate Bitrix24 permission from 'view statistics', so playback for non-admins should use the user's own token to respect it.
- [verified] ((d) recording storage & deletion) Recordings live in Call Details and in Company Drive ('Telephony – recorded calls'). Bitrix24 does not auto-delete recordings; users with the delete permission can remove them (Drive recycle bin keeps them in Call Details; permanent delete removes them; deletion from Call Details is irreversible).
  - src: https://helpdesk.bitrix24.com/open/18651532/
  - impact: Cached RECORD_FILE_ID / CALL_RECORD_URL can go stale (file deleted). The player must handle 404/403 gracefully and the sync should periodically re-read recent rows (or refresh on playback failure) instead of trusting the cached link forever.
- [verified] ((e) request intensity limits) Leaky-bucket per Bitrix24 account: non-Enterprise plans drain 2 requests/sec with a burst threshold of 50; Enterprise 5/sec with threshold 250. Exceeding gives HTTP 503 {"error":"QUERY_LIMIT_EXCEEDED","error_description":"Too many requests"}. Intensity is counted per Bitrix24 instance and per source IP, so multiple apps on one server IP share one bucket. Recommendation: retry with increasing delay, do not run parallel series.
  - src: https://apidocs.bitrix24.com/settings/performance/limits.html
  - impact: Backfill must be serial per portal with a token bucket of ~2 req/s (Basic plan), exponential backoff on 503/QUERY_LIMIT_EXCEEDED. Because the limit is per portal, the scheduler can run portals in parallel but must never fan out inside one portal. Since all tenants come from one Hetzner IP, keep a global concurrency cap too.
- [verified] ((e) time block & operating-time limit) Every response's time{} has start, finish, duration, processing, date_start, date_finish, and (cloud) operating = accumulated execution time of this method for this app/webhook in the last 10 one-minute baskets, operating_reset_at = unix timestamp when the oldest basket drops out. If operating exceeds 480 s per 10 min (cloud; self-hosted default 420 s) the method is blocked with HTTP 429 OPERATION_TIME_LIMIT for all apps/webhooks of that account until baskets expire; recommendation is to suspend calls until operating_reset_at from the last successful response.
  - src: https://apidocs.bitrix24.com/settings/performance/limits.html
  - impact: Persist last time.operating / operating_reset_at per (portal, method); if operating approaches ~400 s pause the backfill until operating_reset_at. On 429 OPERATION_TIME_LIMIT, sleep until operating_reset_at, do not retry immediately. Log time{} with every REST call (also satisfies the 3-day log requirement).
- [verified] ((e) batch) batch executes up to 50 sub-requests (more => ERROR_BATCH_LENGTH_EXCEEDED, HTTP 400); nested batch is prohibited. halt=0 runs all and collects errors in result_error; halt=1 stops at the first error. HTTP 200 response: result.result{cmd:...}, result.result_error{cmd:{error,error_description}}, result.result_next{cmd:start}, result.result_total{cmd:total}, result.result_time{cmd:time{}}. Sub-results can be referenced with $result[cmd][field]. The batch HTTP call itself is one request for intensity and is 'not accounted for in operating', but each nested method is executed separately and its execution time counts toward the per-method operating limit.
  - src: https://apidocs.bitrix24.com/settings/how-to-call-rest-api/batch.html
  - impact: Backfill can pack up to 50 voximplant.statistic.get pages (start=0..2450) into one batch = 2500 rows per HTTP request, cheap on the 2 req/s bucket, but it still burns operating time 50x; watch result_time per command and slow down. Check result_error per command (halt=0) and handle QUERY_LIMIT_EXCEEDED / expired_token inside result_error, not only at HTTP level.
- [verified] ((e) error envelope) REST errors are JSON {"error": "<CODE>", "error_description": "..."}; documented: 401 expired_token ('The access token provided has expired'), 401 NO_AUTH_FOUND, 403 ACCESS_DENIED, 403 insufficient_scope, 503 QUERY_LIMIT_EXCEEDED, 400 ERROR_BATCH_LENGTH_EXCEEDED, 500 INTERNAL_SERVER_ERROR.
  - src: https://apidocs.bitrix24.com/error-codes.html
  - impact: Client should branch on the JSON 'error' string first, HTTP status second. expired_token => single refresh+retry (brief rule 4). insufficient_scope (e.g. disk.file.get without disk scope) must surface as a config error, never retried.
- [verified] ((e) token refresh) Refresh: https://oauth.bitrix.info/oauth/token/ with grant_type=refresh_token, client_id, client_secret, refresh_token. Response includes a NEW access_token and NEW refresh_token ('Retain it instead of the previous one'), expires_in=3600, client_endpoint, server_endpoint, domain, member_id, scope, status, user_id. Access token lifetime 1 hour; refresh token lifetime 180 days; docs recommend reactive renewal on 401 expired_token rather than scheduled renewal. An expired refresh_token requires the full authorization again (i.e. a user opening the app).
  - src: https://apidocs.bitrix24.com/settings/oauth/auto-renewal.html
  - impact: Store client_endpoint from the refresh response and use it as the REST base (on-premise safe). Because background sync uses the installer's refresh token, a portal idle for >180 days loses sync until an admin reopens the app; store 'last_refreshed_at' and surface a 'reauthorize' state. Update stored tokens atomically to avoid two workers racing with the same refresh token.
- [verified] ((f) late arrival) telephony.externalCall.register accepts CALL_START_DATE (ISO-8601 with tz; default = current server time) and the statistics row is created only when telephony.externalCall.finish is called; attachRecord before finish fails with 'Call is not found in the statistic table. Looks like it is not finished yet.' Hence external integrations can create rows whose CALL_START_DATE is earlier than rows already in statistics (backdated register, or long calls finishing late).
  - src: https://apidocs.bitrix24.com/tutorials/telephony/index.html
  - impact: Do NOT key incremental sync on CALL_START_DATE alone; rows can arrive with older start dates. Use the monotonic internal ID as the cursor (">ID": last_id, SORT=ID ASC) which captures late-finished rows regardless of start date. CALL_START_DATE cursor is acceptable only with a generous overlap window and upsert.
- [verified] ((f) rows updated after creation) Recordings are attached after the call: 'call this method after telephony.externalCall.finish when the record is ready' / 'if the recording is not yet ready, call finish without a recording and attach it later'; attachRecord returns FILE_ID and transcriptions are added later via telephony.call.attachTranscription (TRANSCRIPT_ID, TRANSCRIPT_PENDING fields). Thus RECORD_FILE_ID / CALL_RECORD_URL / RECORD_DURATION / TRANSCRIPT_* of an existing statistics row change after the row first appears. The documentation exposes no 'modified date' field on statistics rows and no event for 'record attached'.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/telephony-external-call-attach-record.html
  - impact: Sync must be an UPSERT on (member_id, ID) and must re-scan a trailing window (e.g. rows from the last 24-72 h, or rows with RECORD_FILE_ID IS NULL AND CALL_START_DATE > now-7d) to pick up late-attached recordings; a pure 'only fetch newer' loop will permanently miss recordings. CALL_VOTE and COMMENT updates are undocumented (unverified) but the same re-scan covers them.
- [verified] (OnVoximplantCallEnd event) ONVOXIMPLANTCALLEND (scope telephony, any user can subscribe) fires 'when a call ends and information about the call is recorded in the history' with data: CALL_ID, CALL_TYPE, PHONE_NUMBER, PORTAL_NUMBER, PORTAL_USER_ID, CALL_DURATION, CALL_START_DATE, COST, COST_CURRENCY, CALL_FAILED_CODE, CALL_FAILED_REASON, CRM_ACTIVITY_ID. It does not include ID, CRM_ENTITY_*, CALL_RECORD_URL or RECORD_FILE_ID.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/events/on-voximplant-call-end.html
  - impact: Optional v1.x optimisation: bind ONVOXIMPLANTCALLEND (event.bind, verify APPLICATION_TOKEN) to trigger an immediate incremental sync for that portal instead of polling; do not write the event payload as a row, since it lacks the internal ID and record fields — always re-read via voximplant.statistic.get.
- [verified] (voximplant.url.get) voximplant.url.get (scope telephony, any user) returns links detail_statistics, buy_connector, edit_config, lines — the portal's native telephony pages.
  - src: https://apidocs.bitrix24.com/api-reference/telephony/voximplant/voximplant-url-get.html
  - impact: Cheap way to give a 'open in Bitrix24 call details' link per portal (respects on-premise domains) for the permission-denied state and as a fallback when playback is not possible.

### Corrections to brief
- CRM_ENTITY_TYPE is documented as CONTACT | COMPANY | LEAD only; there is no DEAL value. The CRM_DEAL_DETAIL_TAB placement must map deal -> its contact/company (or match CRM_ACTIVITY_ID to activities bound to the deal), not filter on CRM_ENTITY_TYPE='DEAL'.
- The brief's permission model (portal admin sees all, everyone else sees only own PORTAL_USER_ID) is itself an invented role system: Bitrix24's 'Call statistics' permission has four levels (own / department / any / no access), and 'listen to recordings' is a separate permission. Docs only confirm that a user without the permission gets 403 ACCESS_DENIED; they do not state that the method silently filters rows by own/department level. Either accept and document the simplification, or derive visibility live with the user's own token.
- Incremental sync keyed on last_synced_call_start_date is unsafe: statistics rows are created at finish time and externalCall.register accepts a backdated CALL_START_DATE, so rows can appear with earlier start dates than already-fetched rows. Use the internal integer ID as the cursor (SORT=ID ASC, ">ID": last_id) plus upsert.
- 'Only fetch newer rows' misses later updates: RECORD_FILE_ID / CALL_RECORD_URL / RECORD_DURATION / TRANSCRIPT_* are filled after the row exists (attachRecord is called after finish). A trailing re-scan window with upsert is required.
- CALL_FAILED_CODE has more documented values than the brief's four (200, 304, 486, 603): also 603-S, 403, 404, 480, 484, 503, 402, 423, OTHER. Store as string and map in the UI.
- In the official response example, CALL_RECORD_URL is "" or null even when RECORD_FILE_ID is set; 'has recording' must consider RECORD_FILE_ID too, and the playback spike option (1) may find nothing to point <audio> at.
- Rate-limit numbers: on non-Enterprise plans (i.e. the Basic plan required by this app) the sustainable rate is 2 req/s with a 50-request burst (503 QUERY_LIMIT_EXCEEDED); plus a separate per-method operating-time limit of 480 s per 10 min (429 OPERATION_TIME_LIMIT). Batch is one request for the intensity bucket but each nested command still counts toward operating time. The brief mentions only QUERY_LIMIT_EXCEEDED.
- Refresh token lifetime is 180 days (not mentioned in brief): a portal whose installer token is not refreshed for 180 days cannot sync until an admin reopens the app; plan a 'reauthorization required' portal state.
- Persisted field list should add CALL_CATEGORY, CALL_LOG, TRANSCRIPT_ID, TRANSCRIPT_PENDING (documented fields the brief omits).

### Open questions
- Does voximplant.statistic.get, when called by a user whose 'Call statistics' level is 'own' or 'department', silently return only those rows, or return everything? Official docs do not say. Must be tested on a real portal with a Manager-role user token; this decides whether the app can simply proxy the user's token for reads.
- What does a populated CALL_RECORD_URL actually look like on a cloud portal (host, path, query, signature, expiry, cookie requirement)? No official sample exists; the playback spike must capture a real value and test <audio> from the b24.texnobus.uz origin, and test it again after >1 h and after a token refresh.
- Is RECORD_FILE_ID guaranteed to be a Company Drive file id usable with disk.file.get for all providers (built-in Voximplant, SIP, external REST telephony), and does disk.file.get honour the telephony 'listen to recordings' permission or only Drive folder rights?
- Do CALL_VOTE and COMMENT change after the row is created (e.g. vote given in the call card after hangup)? Undocumented; the trailing re-scan window mitigates it but its size should be tuned from real data.
- Is the internal ID strictly monotonic with row creation across all telephony providers on the portal (assumed, since it is the statistics table primary key)? Verify on a portal with mixed built-in + external lines before relying on it as the sync cursor.
- Does the QUERY_LIMIT_EXCEEDED response carry any Retry-After header or a time block? Docs only give the status/code; capture a real 503 body during load testing.
- Exact operating-time limit on the target on-premise (self-hosted) builds: docs say default 420 s and admin-configurable; the sync throttle should read time.operating adaptively rather than hardcode 480.

## Research block
- [verified] (placement.bind parameters) placement.bind accepts PLACEMENT (string, required), HANDLER (string, required, widget handler URL), TITLE (string, optional; tab name / menu item name), DESCRIPTION (string, optional; 'Not used in practice'), GROUP_NAME (string, optional; grouping for multiple handlers, only some widget types), LANG_ALL (object keyed by language code, each with TITLE/DESCRIPTION/GROUP_NAME), OPTIONS (object; currently used only by messenger widgets, PAGE_BACKGROUND_WORKER and CRM_XXX_DETAIL_ACTIVITY), USER_ID (integer; only PAGE_BACKGROUND_WORKER). Scope: placement (+ scope of the placement, e.g. crm for CRM tabs). Executed by administrator only. Returns result:true/false.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/placement-bind.html
  - impact: Install handler must call placement.bind with the admin's AUTH_ID; there is no GROUP_LABEL parameter (it is GROUP_NAME). OPTIONS is ignored for CRM_*_DETAIL_TAB and LEFT_MENU, so pass only PLACEMENT/HANDLER/TITLE/LANG_ALL. Store the bind result per placement in the portal row so a failed bind can be retried.
- [verified] (placement.bind constraints) 'It is required that the handler URL is definitely accessible from the external network. Links to localhost, local domains ... are not acceptable.' No requirement that the handler be on the app's registered domain is stated. 'You can use the same handler URL for all your widgets. The value that Bitrix24 will report in the PLACEMENT parameter will help determine from which specific placement your handler was invoked.' 'Until the application installation is complete, the registered widgets are not displayed in the Bitrix24 interface — neither to regular users nor to administrators.'
  - src: https://apidocs.bitrix24.com/api-reference/widgets/index.html
  - impact: One handler URL (https://b24.texnobus.uz/app/) can serve DEFAULT, LEFT_MENU and all four CRM tabs; route on the POSTed PLACEMENT value. Call placement.bind before BX24.installFinish(), otherwise tabs do not appear.
- [verified] (placement.bind errors) Error codes: ERROR_PLACEMENT_MAX_COUNT (200), ERROR_PLACEMENT_USER_MODE (200), EMPTY_ERROR_HANDLER_URL (200), ERROR_ARGUMENT (200, required field missing), WRONG_AUTH_TYPE (403, 'Application context required' — webhooks cannot call it), NO_AUTH_FOUND (401), INVALID_CREDENTIALS (403), expired_token (401).
  - src: https://apidocs.bitrix24.com/api-reference/widgets/placement-bind.html
  - impact: Bind must go through the app OAuth token, never a webhook. Treat HTTP 200 + error body as failure (some errors arrive with status 200).
- [verified] (placement.unbind) placement.unbind(PLACEMENT required, HANDLER optional, USER_ID optional). 'If the parameter [HANDLER] is not provided or is empty, the method removes all handlers of the specified placement registered by the application.' Returns result.count. Errors: WRONG_AUTH_TYPE 403, ACCESS_DENIED 403 (not administrator), ERROR_ARGUMENT 400.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/placement-unbind.html
  - impact: Unbind by PLACEMENT only (omit HANDLER) — one call per placement. Requires a valid admin token; on ONAPPUNINSTALL the app token may already be revoked, so unbind failures must be non-fatal.
- [verified] (placement.get) placement.get has no parameters; returns an array of handler objects with placement, userId, handler, options, title, description, langAll. Scope placement. Errors WRONG_AUTH_TYPE 403, NO_AUTH_FOUND 401, INVALID_REQUEST 400 'Https required', expired_token 401, ACCESS_DENIED 403.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/placement-get.html
  - impact: Use placement.get on install/re-install to reconcile which of the five placements are already bound (idempotent install); handler URL must be https.
- [verified] (placement.list) placement.list(SCOPE optional string, FULL optional boolean) returns an array of placement code strings available to the application (current app's scope + global placements unless FULL=true). Errors incl. user_access_error 403 'The user does not have access to the application'.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/placement-list.html
  - impact: Optional pre-check on install: verify CRM_*_DETAIL_TAB and LEFT_MENU are in the list (on-premise portals may lack some) before binding, and log the result.
- [verified] (CRM_*_DETAIL_TAB PLACEMENT_OPTIONS) Placements CRM_LEAD_DETAIL_TAB, CRM_DEAL_DETAIL_TAB, CRM_CONTACT_DETAIL_TAB, CRM_COMPANY_DETAIL_TAB (also QUOTE, SMART_INVOICE, ORDER, CRM_DYNAMIC_XXX_DETAIL_TAB) receive PLACEMENT_OPTIONS as a JSON string: {"ID":"8061","URI":"/crm/deal/details/8061/?any=details%2F8061%2F&IFRAME=Y&IFRAME_TYPE=SIDE_SLIDER"}. ID is 'the identifier of the object whose card the widget is opened in'; URI is universal ('arrives for any placement'). Requires crm scope.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/crm/detail-tab.html
  - impact: Handler parses PLACEMENT_OPTIONS as JSON (it is a string, not an array), derives entity type from PLACEMENT (DEAL/LEAD/CONTACT/COMPANY) and entity id from options.ID (string, cast to int). Map to CRM_ENTITY_TYPE/CRM_ENTITY_ID in the calls table; do not trust URI for the id.
- [verified] (Handler POST payload) 'Bitrix24 sends a POST request to the handler URL and passes the user authorization and the call context in it.' Query: DOMAIN, PROTOCOL (0/1), LANG, APP_SID ('generates a new one each time the widget is rendered'). POST body: AUTH_ID, AUTH_EXPIRES ('Time in seconds after which the authorization token will become invalid'), REFRESH_ID, SERVER_ENDPOINT, APPLICATION_TOKEN, APPLICATION_SCOPE, member_id, status, PLACEMENT, PLACEMENT_OPTIONS (JSON string). status values: EN page lists L local, F free mass-market, D demo, T trial, P paid; RU page lists L, F, S (subscription).
  - src: https://apidocs.bitrix24.com/api-reference/widgets/index.html
  - impact: Every iframe open (including each CRM tab open) is a fresh POST with a fresh APP_SID and current-user AUTH_ID; there is no persistent app session by design. Persist status as a free-form 1-char string (do not enum-constrain). REFRESH_ID arrives on each open, so the per-portal admin refresh token can be re-captured whenever an admin opens the app.
- [verified] (DEFAULT placement) BX24.placement.info() returns {placement, options}; 'If placement receives the value DEFAULT, and an embedding point was expected, it means the widget is opened via the main application link, rather than from a registered point.' Legacy course: 'If on the basic page of the application PLACEMENT equals DEFAULT, then in the widget embed in the left menu it equals LEFT_MENU.'
  - src: https://apidocs.bitrix24.ru/api-reference/widgets/ui-interaction/bx24-placement-info
  - impact: Router: PLACEMENT == 'DEFAULT' (and LEFT_MENU if bound) -> dashboard view; CRM_*_DETAIL_TAB -> entity view; unknown -> explicit error page. PLACEMENT_OPTIONS for DEFAULT carries at most URI.
- [verified] (Left-menu entry for a Market app) Two mechanisms exist. (1) Version card option 'Add your page and item to the main menu' ('Добавлять свою страницу и пункт в главном меню') + 'Application Name in Menu' field: Bitrix24 creates a main-menu item that opens the app's primary handler URL (PLACEMENT=DEFAULT). Placement catalog says LEFT_MENU is 'registered with a method other than placement.bind' — 'Application configurations, no REST API calls.' (2) The dedicated LEFT_MENU page documents placement.bind with PLACEMENT='LEFT_MENU', TITLE, LANG_ALL; item appears in the Applications group of the main menu, 'opens as a separate page — across the entire working area, not in a slider'; PLACEMENT_OPTIONS has only URI; 'Only the employees who have access to the application see the item. Bitrix24 checks access every time it displays the menu.'
  - src: https://apidocs.bitrix24.com/api-reference/widgets/left-menu.html
  - impact: Since the vendor cabinet already registers a left-menu page, the simplest moderation-safe path is to rely on the version-card menu option and handle PLACEMENT=DEFAULT as the dashboard. Do NOT additionally bind LEFT_MENU unless a second menu item is wanted (it would duplicate). Code should accept both DEFAULT and LEFT_MENU for the dashboard so either configuration works.
- [verified] (BX24.js include and init order) Include <script src="//api.bitrix24.com/api/v1/"></script>. BX24.init(callback) 'executes the someCallback function ... after the account data has been retrieved'; 'some actions can only be performed after this data has been received (for example, working with application settings, current user permissions, sending requests to REST, etc.)'. getLang, getDomain, isAdmin, getAuth, fitWindow, resizeWindow, openPath all documented as working 'after BX24.init'.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-init.html
  - impact: Next.js layout must load the SDK script (external, cannot be bundled) and gate every BX24.* call behind a single init promise; render skeleton until init resolves.
- [verified] (BX24.installFinish) BX24.installFinish(): 'Call installFinish as the last step of the install flow — after you have registered the placements and subscribed to the events.' Until invoked, apps with an interface remain uninstalled and receive no events, no placements. Installation wizard: Bitrix24 opens the 'Installation Application Link' in a slider frame on first install; 'After a successful addition, the URL specified in the Installation Application Link field will no longer be called.' app.info INSTALLED=false can verify state.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-install-finish.html
  - impact: /install/ must: persist tokens -> placement.bind x4 server-side -> only then return HTML calling BX24.init(()=>BX24.installFinish()). Install URL is called once per portal; re-install after uninstall calls it again, so the handler must be idempotent (upsert on member_id).
- [verified] (BX24.getAuth) BX24.getAuth() returns {access_token, refresh_token, expires_in, domain, member_id} or false 'if called before initialization or after token expiration'; new tokens auto-generate on next BX24.callMethod() or BX24.refreshAuth().
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-get-auth.html
  - impact: Frontend can obtain a fresh current-user access_token client-side via BX24.getAuth()/refreshAuth() and send it to the backend as the per-request read credential, instead of a long-lived cookie session.
- [verified] (BX24.fitWindow / resizeWindow) BX24.fitWindow([callback]) 'sends a command to change the height of the application frame to match the height of the page content' using BX24.getScrollSize().scrollHeight. BX24.resizeWindow(width, height[, callback]): width int px or '100%', height applied only if > 0. Docs: 'The widget interface is limited by the frame size: a popup wider than this area is cut off, and scrollbars appear at the edges.' No fixed iframe height or lazy-loading rule is documented.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/additional-functions/bx24-fit-window.html
  - impact: Call BX24.fitWindow() after init and on every layout change (ResizeObserver on the root); avoid overlays/dropdowns exceeding frame width; height is content-driven, not fixed.
- [verified] (BX24.placement.info / placement.call) BX24.placement.info() takes no params, returns {placement, options} synchronously, e.g. {"placement":"CRM_LEAD_LIST_MENU","options":{"ID":"1348"}}. BX24.placement.call(command, parameters[, callback]); commands discovered via BX24.placement.getInterface(); for CRM_*_DETAIL_TAB the documented command is reloadData; unknown commands are silently ignored.
  - src: https://apidocs.bitrix24.com/api-reference/widgets/ui-interaction/bx24-placement-call.html
  - impact: Client can re-read entity ID via placement.info without a server round trip; placement.call is not needed for a read-only app (no reloadData required).
- [verified] (BX24.getLang / getDomain / isAdmin / openPath) BX24.getLang() returns one of 'ja, id, ms, de, la, fr, it, pl, br, vn, tr, kz, en, ua, ar, th, sc, tc' (ru not listed on EN page). BX24.getDomain() returns domain without :80/:443 e.g. mycompany.bitrix24.com. BX24.isAdmin() Boolean, client-side. BX24.openPath(path[, callback]) opens Bitrix24 paths in a slider: /crm/deal/details/{id}/, /crm/lead/details/{id}/, /crm/contact/details/{id}/, /crm/company/details/{id}/; callback result 'close' or 'error' with PATH_NOT_AVAILABLE / METHOD_NOT_SUPPORTED_ON_DEVICE; does not work in mobile.
  - src: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/additional-functions/bx24-open-path.html
  - impact: i18n: map LANG from query/getLang to ru/en with fallback to en (treat 'kz' portals as ru-capable via fallback chain). CRM links in the call table should use BX24.openPath with the entity path, not raw hrefs. isAdmin is UI-only; authorization must be decided server-side via user.admin.
- [verified] (user.admin) user.admin returns true 'if the current user has permissions to manage application settings, false otherwise'; scope basic; any user may execute.
  - src: https://apidocs.bitrix24.com/api-reference/common/users/user-admin.html
  - impact: Server-side per-request check with the current user's AUTH_ID (batch user.current + user.admin); cache the result per (member_id, user_id, AUTH_ID) for the token lifetime.
- [verified] (Installation callback (ONAPPINSTALL)) Fires 'when Bitrix24 has created or re-activated the application system user' and for API-only apps. Payload: event='ONAPPINSTALL', event_handler_id, data, ts, auth{access_token, refresh_token, expires_in, scope, domain, client_endpoint, server_endpoint, member_id, application_token}. 'On first installation, check it and only then retain application_token. On subsequent calls, compare the received token with the retained one.'
  - src: https://apidocs.bitrix24.com/settings/app-installation/mass-market-apps/installation-callback.html
  - impact: Store application_token at first sight (install POST or ONAPPINSTALL, whichever first) and reject later events on mismatch; store client_endpoint per portal for on-premise.
- [verified] (App access permissions (product level)) 'After you install a Bitrix24 Market app, all users can access it by default.' 'Only a Bitrix24 administrator can manage app access.' 'Users without access will see an error if they try to open the app.' By default only administrators can install Market apps; admins can enable 'All employees can install apps from Market'.
  - src: https://helpdesk.bitrix24.com/open/19960986/
  - impact: Bitrix24 gates the iframe before it reaches us; the app only needs the admin/own-rows split plus the 'no call statistics permission' state. placement.list may return user_access_error for a user without app access.
- [likely] (Settings URL) The only official settings mechanism found is URL_SETTINGS for the REST-only setup wizard: Bitrix24 POSTs {event: OnAppSettingsInstall|OnAppSettingsDisplay|OnAppSettingsChange, auth{access_token, domain, user_id,...}, data{...}} and expects a JSON form definition back (title, version '1', steps[], form{action, clientId}); Save posts to form.action expecting {'status':'success'} or HTTP 400 {'status':'error','errors':[...]}. No official page describing a PLACEMENT value like 'SETTINGS' or an HTML settings page for apps with an interface was found.
  - src: https://apidocs.bitrix24.com/settings/app-installation/mass-market-apps/rest-only-installation-master.html
  - impact: Do not build /settings/ as an HTML iframe page on assumption. Either leave the field unused (v1 has no settings) or implement it as the JSON form-builder endpoint. Confirm in the vendor cabinet what the 'Settings URL' field actually is before coding.
- [verified] (Iframe embedding headers) App pages must not send X-Frame-Options: SAMEORIGIN (ALLOW-FROM is deprecated); use Content-Security-Policy 'frame-ancestors' listing the portal domain, e.g. "frame-ancestors 'self' https://your-domain.bitrix24.com". No official guidance on cookies/SameSite/session handling in the iframe was found on apidocs or helpdesk.
  - src: https://apidocs.bitrix24.com/local-integrations/site-does-not-allow-connection.html
  - impact: Caddy/Next must drop X-Frame-Options and emit frame-ancestors with the per-portal DOMAIN (or a wildcard list covering *.bitrix24.* plus on-premise hosts, which is impossible statically — prefer per-request CSP from DOMAIN, or omit the header). Because every open is a POST with fresh AUTH_ID, design the app to be cookie-free: backend issues a short-lived signed token derived from the POST and the SPA sends it as a bearer header; if a cookie is used it must be SameSite=None; Secure and treated as optional.
- [verified] (Moderation technical requirements) 'The server part of the solution must use a valid SSL certificate, at least a Domain Validation (DV) Certificate.' 'Solutions making requests to the Bitrix24 REST API from the server must keep logs of API requests and responses for at least the last 3 days.' 'The solution must work correctly in the on-premise Bitrix24.' Post-install may not show 'an empty page within the frame or a page with content without automatic redirection'. Interfaces cannot be 'simple HTML'; security scanner findings are grounds for rejection.
  - src: https://apidocs.bitrix24.com/market/preparing-to-publish/common-requirements.html
  - impact: Add a rest_log table (member_id, method, request, response, status, ts) with 3+ day retention job; install page must redirect/close via installFinish, not show a blank frame; never hardcode bitrix24.com — use client_endpoint from the POST/callback.

### Corrections to brief
- placement.bind has no GROUP_LABEL parameter; the documented parameter is GROUP_NAME (also inside LANG_ALL). Source: https://apidocs.bitrix24.com/api-reference/widgets/placement-bind.html
- The brief lists 'plus a left-menu page' as a placement. The vendor-cabinet option 'Add your page and item to the main menu' opens the Handler URL with PLACEMENT=DEFAULT and is not registered with placement.bind; the separately documented LEFT_MENU placement (placement.bind PLACEMENT='LEFT_MENU') would create a second menu item. Pick one; code should treat both DEFAULT and LEFT_MENU as the dashboard. Sources: https://apidocs.bitrix24.com/api-reference/widgets/index.html and https://apidocs.bitrix24.com/api-reference/widgets/left-menu.html
- Brief says the Settings URL is an app page. The only officially documented settings endpoint (URL_SETTINGS) is a REST-only JSON form-builder contract (POST with event OnAppSettingsInstall/Display/Change, JSON form config in response), not an HTML iframe page. No official doc found for a 'SETTINGS' PLACEMENT value for interface apps. Source: https://apidocs.bitrix24.com/settings/app-installation/mass-market-apps/rest-only-installation-master.html
- Brief's uninstall step 'placement.unbind' requires an admin app token (ACCESS_DENIED/WRONG_AUTH_TYPE otherwise); at ONAPPUNINSTALL time the app's tokens may already be invalid, so unbind must be best-effort and the real cleanup is marking the portal inactive and deleting rows. Source: https://apidocs.bitrix24.com/api-reference/widgets/placement-unbind.html
- Brief's install flow order should be: store tokens -> placement.bind (server-side, admin AUTH_ID) -> return HTML with BX24.init(()=>BX24.installFinish()). Docs explicitly require installFinish to be the last step and widgets are invisible until it runs. Source: https://apidocs.bitrix24.com/sdk/bx24-js-sdk/system-functions/bx24-install-finish.html
- The 'status' POST field has two documented value sets (EN: L/F/D/T/P; RU: L/F/S). Store as an unconstrained 1-char string rather than an enum.

### Open questions
- What exactly is the 'Settings URL' field in the vendor cabinet for an app with an interface: is it opened in an iframe with a POST (and with which PLACEMENT value), or is it the REST-only URL_SETTINGS JSON contract? No official page found; verify in the cabinet / by testing on a dev portal.
- Does Bitrix24 automatically remove placement handlers on uninstall, making placement.unbind in the ONAPPUNINSTALL handler unnecessary? Not stated on official pages fetched.
- No official guidance exists on cookies/SameSite/session handling inside the Bitrix24 iframe; the safest assumption is a cookie-free, per-open bearer-token design. Confirm behaviour in Safari (ITP) during the placement spike.
- Is BX24.getLang() able to return 'ru'? The EN page's list omits 'ru' (lists 'kz', 'ua', etc.); confirm on a Kazakhstan-region portal what LANG arrives in the query string.
- When a CRM card is opened in the SIDE_SLIDER, is the DETAIL_TAB iframe loaded lazily (on tab click) or eagerly? Not documented; affects fitWindow timing and API call volume.
- Whether the version-card 'Add your page and item to the main menu' option and a placement.bind LEFT_MENU registration coexist as two items or one; verify on the test portal.

